import React from "react";
import DetailContainer from "../DetailContainer";

const VariantSection = () => {
  return (
    <DetailContainer title="Variants">
      <p className="text-gray-600">
        Offer your customers different options for price, version, format,
        license type, size, shape, etc.
      </p>
    </DetailContainer>
  );
};

export default VariantSection;
